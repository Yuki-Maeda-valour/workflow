# 基準コミットの記録と代替基準

**この文書の手順の最初に `export GIT_NO_LAZY_FETCH=1` を実行する**(以後の全 git 呼び出しに効く。環境変数は同一シェル内でしか持続しないので、シェルを分けるたびに実行し直す。promisor remote(partial clone)の構成では、git が欠けているオブジェクトを読もうとした時点で遅延取得が走り、`remote.<名>.uploadpack` の設定値がコマンドとして実行される。この変数は git 2.45 以上で効く。2.45 未満で promisor 構成のリポジトリは、下の事前検査が exit 22(承認では復帰できない)で止める — 事前検査より前に打つ git は冒頭の判定の `rev-parse --git-dir` / `rev-parse --show-toplevel` だけで、オブジェクトを読まない)。**対象のディレクトリ(`root`(parent-child)または管理ルート)が git リポジトリでないとき(`git -C <そのディレクトリ> rev-parse --git-dir` が失敗し、**かつ**そのディレクトリ(`CDPATH= cd -P -- <そのディレクトリ> && pwd -P` で物理パスへ正規化してから辿る)からルートまでのどの祖先にも `.git` — ファイル・ディレクトリ・symlink(リンク切れも含め `[ -e ] || [ -L ]`)— が無い)は、この文書の手順(事前検査・基準の記録)を行わない**(**`--cwd` に渡すのは `root`(parent-child)または管理ルート = リポジトリのトップである前提なので(profile の `root` は parent-child = 本体ソースが独立 git のときだけ意味を持ち、single / monorepo では `.` — design §4)、`git -C <そのディレクトリ> rev-parse --show-toplevel` の物理パスが `<そのディレクトリ>` の物理パスと一致しないときは、基準行の有無を問わず停止して両者を報告する(**この不一致の判定は、下の事前検査(`--precheck`)より先に行う**)。`.git` ファイル / symlink が別 repo を指す構成は toplevel が一致して通る — 塞がない限界(external-runners.md §9-1 の縮退判定と同じ理由)** — parent-child で本体ソースの `.git` を消すと `--show-toplevel` が管理ルートを返し、本体側の変更が管理ルートからは gitignore されて 1 件も見えなくなる。実測) — 基準なしのまま Phase 0 を続け、Phase 4 の diff スナップショットを生成できないことを報告する(非 git は停止の理由にしない。external-runners.md の縮退条件と同じく、外部委託だけが内蔵へ縮退する)。`rev-parse --git-dir` は成功するが `rev-parse --show-toplevel` が失敗するときは、`--git-dir` の値を見て、**git ディレクトリ自身(`.` を返す真の bare)のときだけ非 git と同じ扱いにし、作業ツリーを持つレイアウト(`.git` を返す = `core.bare=true` を後から足した等)のときは非 git と扱わず、基準行の有無を問わず停止して報告する**(`core.bare=true` の 1 行で検査を丸ごと飛ばせないようにする。`--is-bare-repository` は両者とも true を返すので識別に使わない。実測)。**ただし、タスク MD に基準行(`> **基準コミット**:`)があるのに非 git と判定されたときは、非 git と扱わず停止して報告する**(基準行は git リポジトリだったときにしか書かれない。リポジトリを書ける者が `.git` を消す・bare に変えるだけで、検査と diff スナップショットを丸ごと飛ばせる)。**`.git` があるのに `rev-parse` が失敗するとき(未知の `extensions.*`・壊れた config 等)は、非 git と扱わず停止して報告する** — `.git/config` に 1 行足すだけで全 git 操作を失敗させ、非 git 扱いで検査を飛ばせる(実測)。git リポジトリのときは、下の事前検査が exit 0 で通った後に、対象リポジトリの toplevel を 1 回だけ解決する(`TOP=$(git -C <root(parent-child)または管理ルート> --no-pager --no-replace-objects -c core.quotePath=false -c core.fsmonitor= -c core.hooksPath=/dev/null -c core.ignoreCase=false rev-parse --show-toplevel)`(冒頭の非 git 判定の `rev-parse` も同じ前置き)。**この文書と do-task Phase 0(手順 3 の `git status` を含む)の全 git 呼び出しは `git --no-pager --no-replace-objects -c core.quotePath=false -c core.fsmonitor= -c core.hooksPath=/dev/null -c core.ignoreCase=false` を前置きにし、`git status` と `git diff` には `--ignore-submodules=dirty` を、`git log` / `git show` には `--no-show-signature` も付ける**(`--no-show-signature` はローカル config の `log.showSignature=true` + `gpg.program`(`gpg.format` に応じて `gpg.ssh.program` / `gpg.x509.program`)で、`gpgsig` ヘッダを持つ commit(`hash-object -t commit` で誰でも作れる)を表示する `log` / `show` がそのプログラムを起動するのを防ぐ — 前置きだけでは防げない。実測。署名の検証自体を行わない指定なので `gpg.format` を問わない)(`--no-replace-objects` は `git replace` による基準 tree のすり替えを無効化する — 置換参照があると基準 SHA・HEAD・index を変えずに `git diff <base>` の比較元が差し替わり、追跡差分が「(差分なし)」になるため)(サブモジュールの作業ツリーの状態まで調べる既定の動作は、サブモジュール側の設定に仕込まれた clean フィルタを実行する。diff-snapshot.sh と同じ理由 — 外部 implementer が残したローカル設定の `core.fsmonitor` が、保護されたスクリプトに到達する前の `git status` / `diff` / `ls-files` で発火して index を書き換える)。さらに **Phase 0 の最初の git 操作(手順 3 の `git status`)より前に `bash {do-task の}scripts/diff-snapshot.sh --cwd <root(parent-child)または管理ルート> --precheck` を実行し(`--cwd` には `TOP` ではなく、解決前のディレクトリをそのまま渡す — `core.worktree` がリポジトリ配下の別ディレクトリを指していると `rev-parse --show-toplevel` は差し替え先を返し、それを渡すと包含検査が差し替えを見逃す)(stdout はリポジトリ外の一時ファイル — `mktemp` の物理パスが `<root(parent-child)または管理ルート>` の物理パス配下なら停止(この時点で `TOP` は未解決。冒頭判定で `--cwd` が toplevel の配下にあることは確認済みで一致までは保証しないので、`TOP` の解決後に `$TOP` 配下でないことを再検査する)、読了後に削除 — へ向け、rc を先に検査する — プロセス置換 `< <(…)` は rc を検査できないので使わない)、exit 0 のときだけ続行し、承認済みの filter があればその一時ファイルを読んで無効化を export する(下記)。それ以外(22 = 改竄の疑い、2 / 20 = 検査自体が成立していない、126 / 127 = スクリプトを起動できない、契約表に無いコード)はすべて停止して終了コードと stderr を報告する**(検査が成功していないのに `git status` へ進むと、事前検査で防ぐはずの clean フィルタが起動する。`--precheck` が返すのは 0 / 2 / 20 / 22 で、21 は返さない。承認前の前置きはフック / fsmonitor を止めるが clean フィルタは止めない)。**exit 22 で停止したら、stderr の全行(パスまたはキー名・理由・内容ダイジェスト。設定の行は表示用の値も)をユーザーに提示する(行が多いときは、理由ごとの件数と先頭 20 行を示したうえで、全行を参照できる形でリポジトリ外に残す)。ユーザーが、提示した一覧の全体を自分の意図した設定だと明示的に承認したときに限り、stderr の最後の `承認ダイジェスト:` の値を `--accept <承認ダイジェスト>` として付けて事前検査を再実行でき、exit 0 になれば続行する。承認はこのセッションのユーザーの発話からだけ取り(profile・リポジトリ内のファイル・過去のログから取らない)、承認した一覧と承認ダイジェストを報告に残す。承認した値は、そのセッションで最後に承認した値になる(承認の場所は問わない — この事前検査・Phase 3 の外部の手順・Phase 4 の diff スナップショットのどこで承認しても。external-runners.md §12-1)。Phase 4 の diff スナップショット生成には、最後に承認した値の `--accept` を付ける。承認が無ければ続行しない。**承認は filter の実行の許可ではない**: 承認した一覧に `filter.<…>` のキーか `filter 属性: <値>` の行があるときは、その filter 名(diff-snapshot.sh と同じ抽出規則。**do-task は名前を自前で組み立てず、`--precheck` が **stdout** に機械可読で出す `GIT_CONFIG_COUNT` / `GIT_CONFIG_KEY_<n>` / `GIT_CONFIG_VALUE_<n>` を、リポジトリ外の一時ファイルに受けて rc を先に検査したうえで `read -r -d ''` で 1 レコードずつ受けて `export "$レコード"`(`NAME=VALUE` 形。`eval` や文字列組立は使わない — filter 名は攻撃者が `.gitattributes` で決めるのでシェルメタ文字を含みうる)する**)を、これ以後の Phase 0 の git 呼び出しで無効化する。**`-c filter.<名>.clean=` は使わない**(名前に `=` を含むと最初の `=` で key/value が割れて空振りし、承認後の `git status` で clean が実行される — 実測)。代わりに diff-snapshot.sh と同じく `GIT_CONFIG_COUNT` / `GIT_CONFIG_KEY_<n>` / `GIT_CONFIG_VALUE_<n>` を export し(`clean` / `smudge` / `process` を空・`required` を `false`)、export の後に名前ごとに `clean` / `smudge` / `process` の 3 キーが `rc=0; v=$(git … config --get "filter.<名>.<変数>") || rc=$?` で「rc 0 かつ空」であることを確かめ、1 つでも満たさなければ続行しない(承認した filter のコマンドが参照するリポジトリ内のファイルは、承認の後でも書き換えられる。diff-snapshot.sh も承認した filter を実行せず実内容で比較するので、提示のときに「承認すると、clean フィルタで伏せている内容が diff スナップショットに出る」ことも伝える)。疑いの一覧が承認時から 1 行でも変わると承認ダイジェストは一致しなくなるので、その場合は新しい一覧を提示して承認を取り直す。`承認ダイジェスト:` の行が出ない exit 22(`core.worktree` の差し替え・`ident` / `working-tree-encoding` 属性・git 2.45 未満での promisor 構成)は承認では復帰できない**(promisor 構成は git を 2.45 以上にすれば NOTE 付きで続行できるので、その旨を報告する。`ident` / `working-tree-encoding` 属性を使うリポジトリではこの事前検査が常に exit 22 になり do-task が着手できないので、その旨と回避 = 該当属性を外すこと をユーザーに報告する。正当な filter や textconv、ローカルの `include.path` を使うリポジトリが恒久的に止まらないための、人の判断による唯一の復帰経路)。**HEAD・空ツリー・未追跡一覧・Phase 4 の手順 1 の diff はすべてこの `TOP` で取る** — 管理ルートと本体が別リポジトリの parent-child で、管理側の sha を本体側の diff に渡すと no-base で止まる)。**デフォルトブランチ**は次の順で解決し、**比較用の完全な ref `DEF_REF`** と **名前比較用の短い名前 `DEF_NAME`** を分けて持つ: ① `git -C "$TOP" symbolic-ref --quiet refs/remotes/origin/HEAD` が取れれば `DEF_REF` = その値(`refs/remotes/origin/<名前>`)、`DEF_NAME` = その値から接頭辞 `refs/remotes/origin/` だけを取り除いた残りの全体(`release/stable` のように `/` を含む名前を末尾の要素へ切り詰めない。ローカルに同名ブランチが無くても `DEF_REF` はそのまま使える)② 取れなければ `git -C "$TOP" show-ref --verify --quiet refs/heads/main` → 無ければ `refs/heads/master` の順にローカルブランチの実在を確認し、最初に見つかったものを `DEF_REF` = `refs/heads/<名前>`、`DEF_NAME` = その名前にする。どちらでも取れなければ『解決できない』として条件 ⑤ を不成立とし、代替基準の候補はユーザー指定だけにする。**条件 ⑤ と自動候補の `rev-list` / `merge-base` には `DEF_REF` を渡し**、「現在のブランチがデフォルトブランチか」は `git -C "$TOP" branch --show-current` と `DEF_NAME` の比較で判定する(`-C "$TOP"` が無いと parent-child で親のブランチ名と比較してしまう — 実測)。**`git rev-list --count "$DEF_REF"..HEAD` / `git merge-base "$DEF_REF" HEAD` が失敗(rc ≠ 0)したら空履歴と扱わず『基準不明』へ進む**(失敗を空と読むと、途中実装済みの HEAD を新規着手の基準に記録してしまう)。(ship-task の「現在のブランチ名が main / master か」の判定とは目的が違う)タスク MD のヘッダに `> **基準コミット**: <sha>` の行が無く、かつ**新規着手と確認できる**(① `- [x]` が 1 つも無い **かつ** ② 追加修正記録に「記録あり」の行が無い(設計レビューの行と保留の行は数えない。「記録あり」の定義・節の範囲・設計レビューの行・保留の行は [../../create-task/references/task-template.md](../../create-task/references/task-template.md) の記法の規約。見出し・段落・表も記録として数える。create-task が設計レビューの行を書き、ship-task の無人モードが do-task より前の停止で保留の行を書くので、これらを数えると自動の新規着手の経路が死ぬ)**かつ** ③ `.claude/reviews/` に **do-task 自身の生成物**(`diff-{TASK_NAME}-iter*.md`、`implementer-*`・`reviewer*-{TASK_NAME}-iter*.md` 等の役割ログ)が無い(**create-task が置く `create-task-{TASK_NAME}-iter*.md` は数えない** — 設計段階の記録で、これを数えると create-task を経た全タスクで条件 ③ が常に不成立になり自動の新規着手経路が死ぬ。**Phase 0 自身が置く `base-{TASK_NAME}-untracked.z` も数えない** — 保存先に symlink や FIFO が先に置かれていても新規着手の保存経路に入り、下の保存先の検査が停止させる。**③ は design §5-18 ① の例外**: ログが有るときだけ保守側〈『基準不明』〉に倒し、無いときの判定は ③ を持たない判定と同じになる — 前回の do-task が対象ファイル表の外に未追跡のファイルを作ったまま基準行だけが消えた場合に、③ が無いと新規着手と判定され、そのファイルが基準時点の未追跡一覧に入って差分から消えるため。ログが無い新しい worktree には前回の未追跡ファイルも無いので、無い側に振れても害は無い)**かつ** ④ `git -C "$TOP" diff --name-only -z --no-renames --ignore-submodules=dirty HEAD`(HEAD が無ければ空ツリーの sha を渡す。`--no-renames` は rename の移動元を落とさないため、`--ignore-submodules=dirty` は `diff.ignoreSubmodules=all` / `submodule.<名前>.ignore=all` の環境でサブモジュールの変更が列挙から落ちるのを防ぐため)と `git -C "$TOP" ls-files -o --exclude-standard -z`(いずれも 1 レコード = 1 パス。`status --porcelain` は rename / copy で 1 レコード 2 パスになり素朴な解析がずれるので使わない)の出力に**タスク MD の対象ファイル表のパスと一致する**未追跡・変更が無い(stat キャッシュで見えない変更 — 同じサイズ・同じ mtime の書き換えや `core.trustctime=false` / `core.checkStat=minimal` — は ④ を通るが、基準 = HEAD に対する Phase 4 の差分には内容の比較で載るので隠れない — 限界)**かつ** ⑤ 現在のブランチがデフォルトブランチ**ではなく**、`git -C "$TOP" rev-list --count "$DEF_REF"..HEAD` が rc 0 で `0` を出す(作業ブランチにまだ commit が無い。**表示用の `git log` の出力が空かどうかで判定しない** — ローカル config の `format.pretty=format:` 1 行で commit があっても出力が空になり、途中実装済みの HEAD を新規着手の基準に記録してしまう(実測)。`rev-list` は `format.*` / `log.*` / 色の設定を読まない。**デフォルトブランチ上にいる、または解決できない場合はこの条件は不成立**とみなす — main 上では `main..HEAD` が常に空で、途中 commit 済みの再開を新規着手と誤認するため。ship-task は常に作業ブランチを切るので通常経路は影響しない。**⑤ の例外(同じセッションの /ship-task が作った作業ブランチ)**: 次のすべてを満たすときは、`rev-list --count` の値に関わらず ⑤ を満たしたとみなす(①〜④ はそのまま問う) — 同じセッションの /ship-task が作業ブランチを作り、ブランチ名・作成時の HEAD の sha・起点の確認・レビュー差分の外で PR に入る commit の一覧の 4 項目を報告に残している / 起点の確認が「履歴の中にある」だった(`git -C "$TOP" <前置き> rev-list --count refs/heads/<DEF_NAME>..<作成時の sha>` が rc 0 で `0` を出した。満たさない、またはデフォルトブランチを解決できないときは例外を当てず、従来の ⑤ で判定する) / 一覧が空だった、または /ship-task の一覧の確認で続行を選んだ(無人は確かめないので、続行とみなす。「続行しない」と答えたブランチには当てない) / 現在のブランチがその作業ブランチで、`git -C "$TOP" <前置き> rev-parse HEAD` が作成時の sha と一致する。背景: ローカルのデフォルトブランチが origin より先に進んでいる(タスク MD を commit したが push していない)と、`DEF_REF` は origin/HEAD を優先するので `rev-list --count "$DEF_REF"..HEAD` が 0 にならず、ship-task が作ったばかりのブランチでも『基準不明』になる(無人では毎周すべてのタスクが保留になる)。起点の確認を求めるのは、任意の commit に detached した状態から作ったブランチにも効くと、その commit の実装が基準に入り、レビューの diff から消えるため。報告の値を失ったら(文脈の圧縮)、対話では従来の ⑤ で判定し、無人では失敗扱いにする(『基準不明』の保留より優先する — [../../ship-task/references/unattended-mode.md](../../ship-task/references/unattended-mode.md) の G4)。信頼の置き方: 判定は implementer が触る前(Phase 0)に行う。⑤ が信じるのは `DEF_REF`、この例外が信じるのはローカルのデフォルトブランチの履歴で、どちらもリポジトリを書ける者が動かせる(下の限界と同じ))— いずれか 1 つでも満たさなければ『基準不明』へ。**限界(祖先関係と ref の偽装)**: `DEF_REF`(`refs/heads/*` / `refs/remotes/*` / `packed-refs`)・`.git/info/grafts`・`.git/objects/info/commit-graph` はリポジトリを書ける者が書け、デフォルトブランチを実装 commit へ向けるか grafts の 1 行で祖先関係を偽れば条件 ⑤ は成立する(正当な新規着手も HEAD = デフォルトブランチの先端なので、機構では区別できない。実測)。この判定を信用できるのは、信頼できない実装者がリポジトリに触る前 — do-task は Phase 0 で基準を記録してから Phase 3 で委託し、以後は基準行を再利用する — に限る。基準行の無い MD で実装済みの作業ツリーを開くときは、新規着手と実装済みを機械的に区別できない(①〜③ はリポジトリ内のファイル、④ は commit 済みなら空 — いずれも実装者が整えられる。ref・grafts・commit-graph の偽装も検出しない)。do-task の外部委託経路では Phase 3 の手順 7′ が起動前のタスク MD を完了判定の基準に据え、本文の変更(基準行の削除を含む)を検出したら停止する(引き継ぎ経路では手順 7 が退避コピーから復元する)— 守りはそちらにある(チェック状態と clean 状態だけでは新規と確定しない)。**『基準不明』の扱いは到達経路(`- [x]` の有無・条件 ⑤ の不成立・検証のみモード)を問わず同じで、下の「代替基準」の手続きに従う**。**例外: `git -C "$TOP" rev-parse --verify --quiet HEAD` が失敗する(コミット 0 の)リポジトリでは、条件 ①〜④ は問い、条件 ⑤ だけを免除して基準 = 空ツリーとして新規着手扱いにする**(実装開始前の commit が存在し得ないため『基準不明』へ回さない。一覧の保存はこの場合も行う。条件 ①〜④ のいずれかが不成立なら代替基準の候補(`merge-base` / ユーザー指定)も存在しないので、基準 = 空ツリー・一覧なし(Phase 4 の手順 1 に `--pre-untracked` を付けない)を代替基準として追記する)。この判定は Phase 0 の手順 7 の再開判定より先に行い、手順 7 は、基準行があればそれを基準に再開の中身を復元する — git でないときは基準行なしで、手順 7 の (B)(C) により再開する)なら `git -C "$TOP" rev-parse HEAD`(HEAD が無ければ `git -C "$TOP" hash-object -t tree /dev/null` の空ツリー — `-C` 無しでは別リポジトリや repo 外の値になる)の値で追記し(**基準行の sha は経路を問わず常に完全形** — ユーザー指定・代替基準も `git rev-parse --verify <指定>^{commit}` で正規化してから書く。基準行を機械的に照合する後続処理が同じ形で読めるようにする)、**同時に**基準時点の未追跡一覧を `LIST=<管理ルート>/.claude/reviews/base-{TASK_NAME}-untracked.z` に保存して(**`LIST` へ直接リダイレクトしない** — リポジトリ内のパスなので、事前に置かれた symlink を辿ってツリー外のファイルを上書きし、FIFO なら書き込み待ちで止まる。① **階層ごとに「検査 → 無ければ作成」の順で進め、`mkdir -p` を先に呼ばない**(`.claude` がツリー外ディレクトリへの symlink だと、検査で止まる前にリンク先へ `reviews` を作ってしまう): `<管理ルート>/.claude`、次に `<管理ルート>/.claude/reviews` のそれぞれについて、`[ -L ]` なら停止して報告、存在しなければ `mkdir`(`-p` なし)で作り、存在して `[ ! -d ]` なら停止して報告する(**do-task Phase 0 の手順 5 が最初に行う `.claude/reviews/` の作成も、この順序で行う**) ② `LIST` に既存のエントリがあり(`[ -e ] || [ -L ]`)、それが symlink か通常ファイル以外なら、何も書かずに停止して報告する ③ `tmp=$(mktemp "<管理ルート>/.claude/reviews/.base-untracked.XXXXXX")` へ `git -C "$TOP" ls-files -o --exclude-standard -z > "$tmp"` で書き、git の終了コードが 0 であることを確かめ、sha256 を `$tmp` から計算してから `mv -- "$tmp" "$LIST"` で公開する。どこかで失敗したら `$tmp` を消し、基準行を書かずに停止して報告する)、**その sha256(`sha256sum` 相当。external-runners.md §12-2 と同じ書きぶり)を同じヘッダ行に併記する**(書式: `> **基準コミット**: <sha> / 未追跡一覧: <sha256>`。一覧は `.claude/reviews/` にあり external-runners.md §12-2 の比較対象外なので、外部 implementer がパスを足して成果物を隠せる — タスク MD 側のダイジェストで真正性を担保する)。**一覧を新規に取るのは新規着手のときだけ**(既存タスクへ代替基準を足す場合は当時の一覧が無いので取らず、Phase 4 の手順 1 は `--pre-untracked` を付けない — 実装途中の未追跡成果物が『基準時点から存在』に落ちるのを防ぐ)。**基準行が無く `- [x]` がある(この機能の導入前から進行中のタスクの再開)場合は、現在の HEAD を記録しない** — 実装済みの commit が基準以前に落ちてレビューから消えるため、『基準不明』として代替基準を確認してから追記する。**代替基準は実装開始前の commit に限る**(候補: `git merge-base "$DEF_REF" HEAD` / ユーザー指定 — 「作業ブランチの分岐点」は merge-base と同義なので別候補に数えない。デフォルトブランチの解決は冒頭のとおり(解決できなければ条件 ⑤ は不成立)。**自動候補(`merge-base`)は提示するだけで、採用はユーザーの明示的な指定に限る** — `DEF_REF`(`refs/heads/*` / `refs/remotes/*` / `packed-refs`)はリポジトリを書ける者が動かせるので、基準行の sha を到達不能にして『基準不明』にした上でデフォルトブランチを任意の commit へ向ければ、下の足切り (i)(ii) を通る候補を作れ、採用すればそれより前の実装 commit が差分から消える(実測)。提示には『基準不明』の理由(基準行の sha が到達不能 = 別クローンか、履歴が書き換えられ旧 commit が消えた — 委託の後に起きたなら履歴の改竄を疑う)、候補の sha、候補以後の一覧 `git -C "$TOP" log --no-show-signature --no-decorate --format='%H %s' <候補>..HEAD`(件名の制御文字は `?` に置き換える — `## 改竄の疑い` の表示用の値と同じ規則。50 行を超えたら件数と先頭 50 行を示し、全行を参照できる形でリポジトリ外に残す。`--oneline` は `core.abbrev` で sha が縮み `log.decorate` で攻撃者の ref 名が混ざる — 実測)を添える。**この段落と足切り・注記・候補の算出(`merge-base`)・指定の正規化の git 呼び出しはすべて `git -C "$TOP" <前置き>` で行う**(parent-child で管理ルートの git が親リポジトリの ref を解決し、親の履歴を提示してしまう — 実測。他の呼び出しと同じ規約)。**ユーザーが指定した sha にも同じ提示を行ってから基準行に書く**。**限界**: 提示は判断の材料であって保証ではない — 履歴は実装者が書けるので、候補やその祖先に実装が含まれていても機械的には見分けられない(対象ファイルの履歴は正当な候補でも必ず在り、タスク MD の対象ファイル表には「新規か既存か」を機械的に取れる列が無い)。ユーザーは実装開始前の commit を自分の記録(PR・チャット・commit の日時)から確かめて指定する。足切りと注記: (i) `git rev-parse <候補>^{commit}` と `git rev-parse HEAD` が一致する候補は提示しない — デフォルトブランチ上では `merge-base` が常に HEAD と一致して縮退する。(ii) `<候補>..HEAD` の commit(`git rev-list <候補>..HEAD` で列挙)のうち、現在のブランチ以外の参照にも含まれるもの(`git for-each-ref --format='%(refname:short)' --contains <commit> refs/heads refs/remotes` の出力から**現在のブランチとその `@{upstream}`(未設定なら `git rev-parse --abbrev-ref @{upstream}` が fatal になるので、現在のブランチだけ)を除いて**残りがあれば注記する(積み重ね元のブランチが local・remote とも削除済みだと残りが空になる — 提示に影響しない)。残りがあるときは**積み重ねブランチ**とみなし、その旨を添える(他タスクの未マージ実装が差分に混ざる — 例: 直前のタスクの未マージブランチの先端から切った作業ブランチでは `merge-base "$DEF_REF" HEAD` がデフォルトブランチの先端になり、先行タスクの差分が丸ごと載る)。**候補の採用はデフォルトブランチ上かどうかを問わずユーザー指定だけ**: `git -C "$TOP" log --no-show-signature --no-decorate --format='%H %s' "$DEF_REF"`(同じ置換と上限。`DEF_REF` が解決できないときは代わりに `HEAD` を渡す — 空文字を渡すと `log` が fatal になり材料が出ない。実測)を提示して実装開始前の commit を選んでもらう。**ユーザー指定は HEAD と一致してもよい**(commit 前の新規着手・積み重ねブランチの着手では HEAD こそが実装開始前の commit。「まだ実装 commit が無い」ことをユーザーが確認した上での指定に限る)。**完了記録や実装途中の commit を基準にすると、その時点までの実装が差分から消える** — 完了記録の commit は履歴をたどる手掛かりにだけ使う)。基準行が**あれば再利用**し(**ただし (a) その sha が空ツリー(`git -C "$TOP" hash-object -t tree /dev/null`。コミット 0 で着手した基準は commit ではないので、まず文字列の一致比較で通す — `^{commit}` を問うと `--quiet` でも stderr にエラーが出る)と一致せず、かつ `git -C "$TOP" rev-parse --verify --quiet '<基準行の sha>^{commit}' 2>/dev/null` が失敗する(= オブジェクト不在。別クローン・gc 後)なら『基準不明』として扱い、代替基準の手続きで得た sha で基準行を上書き更新して、旧 sha と上書きした事実を報告に残す。検証のみモードでは上書きせず報告だけ** — 放置すると Phase 4 の手順 1 が毎回 exit 4 で止まる袋小路になる。**(b) `git -C "$TOP" merge-base --is-ancestor '<基準行の sha>' HEAD` が rc 1(到達はできるが HEAD の祖先でない = 同じクローンでの rebase / amend / reset、または grafts 等で祖先関係を偽られた)のときは『基準不明』にせず基準行をそのまま再利用する** — 差分は tree 同士で祖先関係に依らず、Phase 4 の手順 1 が `WARNING: 基準は HEAD の祖先ではない` を出すので、その旨を報告して続ける(rebase 後は上流の変更が差分に混ざるが隠れる方向ではない。基準を変えたければユーザー指定で上書きする。`--is-ancestor` の rc 1 は `.git/info/grafts` 1 行で委託の後にも作れるので『基準不明』のトリガーにしない — 実測。rc 2 以上(空ツリー基準・HEAD 無し)も同じ))、**一覧は作り直さない**(再開時に上書きすると初回実装で作ったファイルが『基準時点から存在』になりレビューから消える。一覧が失われていれば再生成せず Phase 4 の手順 1 は `--pre-untracked` を付けずに呼び NOTE を報告する)。揮発キャッシュでなくタスク MD に基準を置くのは再開でも同じ基準から差分を取るため(一覧そのものは `.claude/reviews/` の任意入力で、欠落しても動作は止まらず未追跡集合が広がるだけ — design §5-18 ① と整合)。**検証のみモードではタスク MD へ追記しない** — 基準行があればそれ、無ければ『基準不明』として報告し、**実装開始前の commit**(候補の提示と足切り・注記は上の「代替基準」と同じ — 採用はユーザー指定に限る)を確認してから Phase 4 へ進む。**タスク MD の実体がファイルでない運用(課題管理システムの本文など)では、同じ書式の基準行をその本文のヘッダ部に置く(全文置換する前に、取得した本文の中身と行数を検査する)**
