# Phase 4 の diff スナップショット生成

`loop.sh` の周では、この文書の `$` を含むコマンドの例を字面どおりに打たず、[unattended-mode.md](../../ship-task/references/unattended-mode.md) の「`loop.sh` の周の Bash の書き方」で打つ。

`bash {do-task の}scripts/diff-snapshot.sh --cwd <root(parent-child)または管理ルート> --base <基準コミット> --out <管理ルート>/.claude/reviews/diff-{TASK_NAME}-iter{ITER}.md --exclude-glob <secret_paths の各要素> [--pre-untracked <管理ルート>/.claude/reviews/base-{TASK_NAME}-untracked.z] [--accept <承認ダイジェスト>]` で生成する(対象が git リポジトリでないとき — references/base-commit.md の冒頭の判定 — は実行しない。**`--out` と `--pre-untracked` は管理ルート配下の絶対パスで渡す**(`--cwd` とは基準が別)。`--exclude-glob` は profile の `secret_paths` の要素ごとに 1 つずつ**シングルクォートで**渡す(`--exclude-glob '.env.*'`。シェル展開を避ける)。**渡す前に各要素の内容を検査し、引用符(`'` `"`)・改行・制御文字・バッククォート・`$`・`[` `]` `{` `}` を含む要素があれば、その要素と理由を報告して Phase 4 の手順 1 を実行せず停止する**(profile は commit される共有ファイルで、値がコマンド行の構造を変えてはならない — external-runners.md §1 の `features.runner_models` と同じ趣旨。§1 は `-` 始まりと空白を拒否するが glob では空白が正当なので、拒否する集合は別。除外できない機密指定を抱えたままレビュー経路を走らせない)。**生成後、`## 除外(機密)` に載ったパス(節の各行は `<パス>` + タブ + 属性。`## gitignore により除外(.gitignore 以外)` だけはパスだけの行で、引用の規則は同じ — 照合側はタブが無い行を行全体でパス列として扱う。行頭が `"` ならエスケープされていない最初の `"` までをパス列として取り出して復号し、それ以外は最初のタブまでをパス列として、対象ファイル表のパスと完全一致で比べる)とタスク MD の対象ファイル表の積が空でなければ、そのパスを列挙して報告し、該当項目を未検証として扱う**(secret_paths が広すぎて成果物が除外される経路を塞ぐ。範囲そのものの検査は `diff-snapshot.sh` 側にあり、secret_paths が全パスに当たる指定は exit 2 で拒否される)。**`secret_paths` は作業ツリーの profile の値だけで決めない**: 管理ルートが git リポジトリで、`git -C <管理ルート> show <REF>:.claude/project-profile.yml`(`<REF>` = 管理ルートと `--cwd` の対象が同じリポジトリなら基準コミット、別リポジトリ(parent-child)なら管理ルートの `HEAD`。git の前置きは references/base-commit.md と同じ)が成功する場合は、その `secret_paths` と現在の profile の `secret_paths` の**和集合**を渡す(作業ツリーの profile から要素を消す・空配列にするだけでは機密の除外を外せないようにする — profile の変更は追跡差分に出るが、それを読む時点では機密の内容が既に snapshot と外部レビュー用の一時ツリーへ出ている)。基準側にだけある要素は報告に列挙する。**`<REF>` が空ツリー(対象リポジトリで `git … hash-object -t tree /dev/null` が返す値。SHA-1 では `4b825dc…`、SHA-256 では別の値 — 記録も比較もその都度の計算値で行う)と文字列一致するときは、commit の有無に関わらず基準側を既定 3 要素として数え NOTE で報告する**(commit 0 で着手し初回 commit の後に再開する正常経路 — base-commit.md が空ツリー基準を再利用する。`^{commit}` で解決を問うと tree なので必ず失敗する。基準行そのものの改竄 — 空ツリーへの書き換えを含む — は外部 implementer 経路のタスク MD 保護〈契約 §12-2 の退避と正規化差分〉が検出する前提で、この規則は基準行を信頼する)。**それ以外で管理ルートが git リポジトリなのに `<REF>` が commit に解決できない(`git -C <管理ルート> rev-parse --verify --quiet <REF>^{commit}` が失敗 — rc 1 の未解決も rc 128 の git 自体の失敗も)ときは停止して報告する**(parent-child の管理ルートは commit 済みであることを前提にする。`git init` + `git add` だけの管理ルートも止まり、報告に「管理ルートに commit が無ければ作ってから再実行。commit が在るのに HEAD が解決できなければ `.git/` が書き換えられた疑いなので hooks・config・ref を確認する」を添え、git コマンドは案内しない。commit の無い新規と全 ref を消した改竄を ref・index・`logs/HEAD` の有無や objects の走査で分けない — 前者は消せば迂回でき、後者は FIFO・alternates・改名した pack で戻らないか誤る — 実測)。**parent-child の基準側 profile は管理ルートの HEAD なので、管理ルートへ commit できる者に差し替えられる(通常の add + commit 1 回で `show` が成功したまま基準側を空にできる — 実測)。この経路の和集合が守るのは常時包含の既定 3 要素だけで、基準行に管理ルートの sha を併記する恒久策はスコープ外。空ツリー基準(commit 0 で着手した単一リポジトリ — 基準側は常に既定 3 要素で、作業ツリー profile の書き換えに守りが無い)も同じ限界の内**。`<REF>` は解決できるが profile が無い(基準コミットが profile の導入より前の場合を含む)・管理ルートが git リポジトリでない(`git -C <管理ルート> rev-parse --git-dir` が失敗し、かつ管理ルート(`CDPATH= cd -P -- <管理ルート> && pwd -P` で物理パスへ正規化)とその祖先に `.git`(`[ -e ] || [ -L ]`)が無い)ときは、基準側を既定 3 要素として数え、その旨を NOTE で報告する(`.git` が在るのに失敗する — 未知の `extensions.*`・壊れた config — ときは非 git と扱わず停止する。base-commit.md と同じ向き。判定は git 性 → `<REF>` の解決の順)。内容の検査は和集合の全要素に掛ける。**既定 `[".env", ".env.*", ".dev.vars"]`(design §4)は、取得の可否・キーの有無・空配列を問わず常に和集合に含める**(`secret_paths` は既定に足すだけで既定を外す手段は無い — 外部レビューへ渡す正当な理由が無く、外せる経路は改竄の余地になる。和集合は空にならない)。**許可リストの例外は設けない**(`.env.example` / `.env.sample` / `.env.template` のようなテンプレートも `.env.*` に当たれば `## 除外(機密)` に載り、パッチから除去され、一時ツリーには無い — profile が無い構成でも `secret_paths` が空配列の構成でも同じ。テンプレートの変更が外部レビューの対象外になるのは受け入れる限界)。glob→ERE の変換はスクリプトが行うので手で組み立てない。`--pre-untracked` は一覧ファイルが存在し、かつ基準行に `未追跡一覧: <sha256>` があるときだけ、その値を `--pre-untracked-sha256 <sha256>` に添えて付ける。無人(`--unattended`)で、守る値の未追跡一覧の状態が「無し」なら、一覧ファイルが後から現れても `--pre-untracked` を付けない(../../ship-task/references/unattended-mode.md §7)。**ダイジェストが無い・一致しない場合は一覧を使わず全未追跡を対象にして、その旨を報告する**(stderr が `未追跡一覧のダイジェストが一致しない` の exit 2 に限り **`--pre-untracked` と `--pre-untracked-sha256` の両方を外して**再実行する(片方だけ残すと exit 2)。それ以外の exit 2 は停止))。**exit 0 か 21 のときだけ Phase 4 の突合へ進む。それ以外(2 / 4 / 20 / 22、および契約表に無いコード — スクリプトを解決できない部分導入での 127 等)は Phase 4 を続行せず、終了コードと stderr をそのまま報告して停止する**(「差分なし」と区別する。基準が到達不能なら rebase / amend / reset を疑う)。**exit 22 のときは(無人〈`--unattended`〉では承認を求めず失敗扱い。契約表に無い終了コードも同じ — [../../ship-task/references/unattended-mode.md](../../ship-task/references/unattended-mode.md) の D10)`## 改竄の疑い` の全行をユーザーに提示し、references/base-commit.md の事前検査と同じ承認手順で、ユーザーが承認したときだけ `--accept <承認ダイジェスト>` を付けて再実行できる。最後に承認した値(そのセッションで最後にユーザーが承認した承認ダイジェスト。承認の場所は問わない — Phase 0 の事前検査・Phase 3 の外部の手順・ここでの承認のどれでもよい。external-runners.md §12-1)を最初から付ける。**承認した filter があるときは、`--precheck` が stdout に出した `NAME=VALUE` トークン(`GIT_CONFIG_COUNT` / `GIT_CONFIG_KEY_<n>` / `GIT_CONFIG_VALUE_<n>`)を **リポジトリ外の一時ファイルに受けて rc を先に検査し(0 以外は停止。プロセス置換 `< <(…)` は rc を検査できないので使わない)**、`read -r -d ''` で受けて `export "$トークン"` してから、その git 呼び出しを行う**(`-c filter.<名>.clean=` は名前に `=` を含むと空振りするので使わない。`eval` / 文字列組立は使わない。do-task は filter 名を自前で組み立てない)。**環境変数は同一シェル内でしか持続しないので、Phase 0 と Phase 4 のそれぞれで `--precheck` を呼び直して `export` する**(`--precheck` は冪等。**Phase 4 では、この `--precheck` を、上の `show <REF>:.claude/project-profile.yml` の呼び出しを含むどの git 呼び出しよりも先に行う** — git 2.45 未満では `GIT_NO_LAZY_FETCH` が効かないので、Phase 0 の後で promisor 構成を仕込まれると、`--precheck` より前に打つ git が欠けたオブジェクトを読みに行くだけで設定値のコマンドが実行されうる。`--precheck` はその構成を承認の対象外の exit 22 で止める)。(疑いの一覧が承認時から変わっていれば一致せず exit 22 に戻るので、新しい一覧を提示して承認を取り直す。`承認ダイジェスト:` の行が出ない exit 22 と、承認しても残る `Binary files 検出` の行は、承認では復帰できないので停止して報告する。**`ident` / `working-tree-encoding` 属性による exit 22 も承認で復帰できず、この属性を使うリポジトリでは do-task が着手できない旨と、回避は該当属性を外すこと(生バイト比較への移行は別タスク)をユーザーに報告する。git 2.45 未満での promisor 構成による exit 22 も承認では復帰できず、git を 2.45 以上にすれば NOTE 付きで続行できる旨を報告する(references/base-commit.md の事前検査と同じ案内)。git-lfs を起動できない exit 20(LFS 管理下の追跡ファイルがあり `git-lfs` が PATH に無い)も着手できない旨と、回避は git-lfs の導入を報告する**)**。**exit 21(生成済みだが欠落あり)は `## 含められなかった未追跡` の内容を報告してから突合を続ける**。**`- [x]` に対応する変更が diff に見当たらないときは `## 基準時点から存在(対象外)` を確認する。該当パスがあっても「実装済み」とは扱わず、その項目を未検証として扱う** — 対象外一覧への一致だけでは、着手前から在った未追跡ファイルを触らずにチェックを付けた偽完了を検出できない。**index を触らずに** `--include-untracked <パス>` を付けて Phase 4 の手順 1 を再生成し(そのパスの内容が `## 未追跡ファイル` に出る)、変更が実在するか突合するか、完了条件の実行で確認できるまで、その項目を完了承認に進めない。**対象外節にも無いときは `## 除外(機密)`(大小文字を区別しない照合で機密名に当たった実装ファイルはここにパスだけ載り、`--include-untracked` でも復帰しない — 名前を変えて再実行する)と `## 内容を省略した未追跡`(1 MiB 超・バイナリ・総量上限。1 MiB 超とバイナリは見出しに NOTE が出ず exit 0 のまま)と `## 内容を省略した追跡`(バイナリ・1 MiB 超・作業ツリーが通常ファイルでない。追跡済みファイルはこちら)と `## 含められなかった未追跡`(理由 9 種。exit 0 のものもある)を確認し、該当すれば理由をそのまま報告してその項目を未検証として扱う。どの節にも無いときは `git ls-files -o --ignored --exclude-standard` で gitignore 由来(列挙されない)かを確認する**(**この文書の git 呼び出し(`ls-files` / `log` / `show` / `rev-parse` / `for-each-ref` / `hash-object`)はすべて references/base-commit.md と同じ前置き(承認済み filter の無効化と、シェルの最初の `export GIT_NO_LAZY_FETCH=1` を含む。`log` / `show` には `--no-show-signature` も)を付ける** — 素の `ls-files -o --ignored` でもローカルの `core.fsmonitor` が発火する。実測。結果を報告に残す。`git add` はしない — レビュー経路はユーザーの git 状態を書き換えない(external-runners.md §9))。**見出しに sparse-checkout の NOTE(件数と該当パス)が出たのに、対象リポジトリを sparse-checkout で運用している心当たりがユーザーに無ければ、改竄として扱い Phase 4 を続行しない**(無人では心当たりを確かめられないので失敗扱い — unattended-mode.md の D19)(sparse の設定もパターンもリポジトリ内から書き換えられる)。**見出しに WARNING(基準が HEAD の祖先でない)や NOTE(`core.fsmonitor` / `core.hookspath` / `core.filemode` / `core.symlinks` / `core.ignorecase` / `core.ignorestat` / `core.pager` / `pager.*` を無効化して実行した — キー名は git の出力どおり小文字・追跡ファイルの別表記(同一 inode)の件数・`.git` という名前のエントリの件数・サブモジュール配下の `.git` 名エントリの件数とパス・`.git` 名エントリ・ディレクトリの走査に失敗した経路あり・`.git/info/exclude` あり・総量上限・一覧なし・`.gitignore` / `.gitattributes` の変更・gitignore 済みの未追跡の件数・`filter=lfs` のパスの実内容比較・承認済みとして扱った項目・承認済みの filter を実行せず実内容で比較した・置換参照を無効化して実行・promisor 構成: 遅延取得を無効化して実行・対象外の分類はリポジトリ内の一覧に基づく)があるか、`git -C "$TOP" log --no-show-signature --no-decorate --format='%H %s' <base>..HEAD`(件名の制御文字は `?` に置き換える)に本タスクと無関係な commit があれば、その旨を報告してから突合する**
