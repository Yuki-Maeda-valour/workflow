## Claude Code 固有(他ホストには無関係)

- 検証には本体のプラグイン検証も加える(`claude plugin validate .`)
- このリポジトリを plugin として導入している場合、編集の反映には push → `/plugin marketplace update` → `/plugin update` が要る(作業ツリーの変更は導入済みプラグインに自動反映されない)
